-Comando a ejecutar en consola para generar el ejecutable-

 pyinstaller --onefile --windowed --collect-all tkcalendar --hidden-import=psycopg2 --name "generar_reporte" --icon=clock.ico generar_reporte.py
